services:
  nginx:
    build: ./nginx/
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8000:80"
    depends_on:
      - server
  server:
    build: ./server/